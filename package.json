{
  "private": true,
  "workspaces": [
    "client",
    "server",
    "common"
  ],
  "devDependencies": {
    "elysia": "^0.7.16"
  },
  "scripts": {
    "build:client": "cd client && bun run build",
    "start:server": "cd server && bun run start",
    "start:client": "cd client && PORT=${PORT:-4000} bun --bun build/index.js"
  }
}